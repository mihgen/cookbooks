#!/bin/bash
for daemon in <%= @daemons.join(' ') %>; do
  file="<%= @scripts_dir %>/$daemon.sh"
  [ -f $file ] && { echo "--------- $daemon $1 ---------"; /bin/bash $file $1; } 
  sleep 1
  echo
done
